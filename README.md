# Daykeeper MCP

The official Model Context Protocol server for safe Daykeeper administration
by agents. It is published as `@skyporch/daykeeper-mcp` after its tool contract,
OAuth model, and public-release gates are approved.

This repository is private and no npm package has been published yet.
